# Frontend (Next.js)

Kurzanleitung für den lokalen Entwicklungsstart.

## Starten

```bash
cd frontend
pnpm install
pnpm dev
```

Das Frontend läuft danach auf `http://localhost:3000`.

## Hinweis

Die App erwartet das Backend auf `http://localhost:8000`.
